# Decision Log

## 2026-08-18 — Session 1
1. **WIP queue = Turn Over The Flag / Manic / Long Long Drive.** Rationale: flagship follows Jeff's own momentum (v1.6 mixed Mar 2026, 2 overdubs left) rather than raw potential score (TBFH scored higher at 86 but sits at v1.4 with 3 items; it's next in line). Quick win = Manic (single remaining overdub). Experimental = Long Long Drive (highest emotional ceiling, arrangement half-designed in the doc).
2. **Covers excluded from ranking** — 93 identified via setlists (the 4-26-24 sheet names artist for every song) and title recognition; Trailer Swift folder = all Taylor Swift; "06 - First Date" in LYRICS folder is the Blink-182 song despite sitting among originals.
3. **"Over 18" (2010) kept separate from "It's Only Been 18" (2023)** — different subjects, 13 years apart; titles coincidental.
4. **"Mom" (SD 2011) merged with "Mom, You're The Bomb"** as one entity, medium confidence — same subject (Connie Story), same catalog thread; pending Jeff confirmation. Co-writer Greg Baldia recorded.
5. **dustduff90 "Structures" songs → UNK-, frozen** — strong material but zero appearances in Jeff's own folders; no ranking, no external use, until authorship is clarified.
6. **Voice Memos intake via Mac folder** rather than blocking the project on iPhone access.
7. **Assumption:** the 6-bounce sequence supersedes the 14-track Prime Outline tracklist (Oct 2024) as the current album plan — inferred from file activity, not confirmed by Jeff.

## 2026-08-18 — Session 1, Pass 3 (Jeff's rights answers + Voice Memos)
8. **The Way I Love You** reclassified: composition by Paco Estrada, given to the band (Jeff, verbatim: "ours as paco gave it to us"). Stays album track 2; leaves the Jeff-originals ranking; Paco credited on release. No AI/external upload without revisiting rights.
9. **Better Than Now** reclassified: written by Sean, Stalemate's bassist. Lyric-capture task → Sean. No external use without his consent.
10. **UNK-0001..0013 = Dustin Duffy's songs** (Jeff confirmed). Relabeled "Dustin Duffy (Stalemate Split)". His stems folders (Aug 2025, with BPMs) show the Split is ACTIVE.
11. **Long Long Drive confirmed Jeff's** — rights high.
12. **Trailer Swift = Jeff's solo project**; Rad Dad and Stalemate also play those songs live. Compositions remain covers; the 13-track finished album (Jul 2025) is Jeff's work product.
13. **Voice Memos pulled via Finder (computer use), 916 files copied to intake** — originals untouched. DB titles extracted; 326 memos auto-matched to 73 songs; 590 unmatched (~290 are untitled location-named ideas = hidden-gem pool).
14. **The Prime Outline upgraded from "title only" to developed demo** (10 memos + J&C v2.1 wav + v1.3 logicx).

## 2026-08-19 — Session 1, final passes
15. **Workbench APPROVED by Jeff** ("ok awesome lets put this in here"): Whisper + Demucs + Basic Pitch + Chromaprint + Matchering on the mac mini. Install = Jeff pastes one Terminal command (in Workbench Plan); post-install pipeline defined. Standing scout order: propose new open tools each session before installing.
16. **Memo-to-demo pipeline defined** (find → Jeff 30s listen → prep → private Suno sketch → judge → human recording path). Mystery Gem #1 (untitled memo 2023-09-02, Em ~144, top music-score) delivered to Jeff — awaiting his gem/meh verdict.
17. **Songwriting-partner layer live**: Song Map (7 threads), Style Guide (binding co-write rules), co-writer .skill delivered, Pocket Vault pinned for iPhone use via the Claude app inside this project.
18. Jeff's Manic verdict logged (lead guitar + ska/horns idea → EXP-002).

## 2026-08-19 — Session 2
19. **9-3-23 hidden-gem batch listened, verdict: not recognized.** Jeff listened to the top-ranked 2023-09 memos (incl. Mystery Gem #1, "1922 Maxwell Dr 113," 2023-09-02) and doesn't recognize any of them. Logged as genuinely forgotten fragments, not a match to a known song — none promoted to a catalog entity. No red flag on the triage heuristic; this is expected (untitled memos are a real grab-bag). Not pursuing further Suno demo work on these until Whisper transcripts exist to search by lyric instead of by ear.
20. **Workbench install run by Jeff** ("ran this install") — awaiting confirmation of success/errors before starting the post-install pipeline (Whisper pass, Chromaprint pass, Demucs on Blue Skies Fade, etc.).
21. **Workbench install confirmed clean by Jeff.** Delivered `whisper_pass.py` — a resumable script Jeff runs once in his own Terminal to transcribe all 916 Voice Memo Intake files into `03 Voice Memos/Transcripts/`. Originals untouched; runs fully local; safe to leave running for hours/overnight. Once done, next session pivots the hidden-gem hunt from blind listening to lyric search, and captures the Better Than Now lyric for Sean.
22. **Whisper pass started on the mac mini** (base.en model, running locally over all 916 Voice Memo Intake files → `03 Voice Memos/Transcripts/`). In progress as of this session; will likely run for hours. Next session after it finishes: read the Transcripts folder, search for hidden gems by lyric, capture Better Than Now's lyric for Sean, and re-attempt matching the ~290 still-unmatched memos.
23. **Whisper pass ~93% done (850/916), zero errors.** Spot-checked output quality: real transcription, but Whisper loops/stutters on hummed non-lyric passages (repeats a word/phrase many times) — expected base-model behavior on melody-only sections, not a install problem. Early payoff: found "20240725_221716-3CDA898A.txt" containing "Better than now!" — likely raw material toward Sean's Better Than Now lyric (Needs Jeff #2). Flagged for a real listen once the full pass is done, not treated as a clean lyric capture yet (transcript is messy/looped in spots).

## 2026-08-20 — Session 2, the Transcript Pass (Fable model, deep pass at Jeff's request)
24. **Whisper pass complete: 915/916 transcribed, 1 corrupt** (2025-04-26 memo, truncated recording — unrecoverable by rerun). Transcripts bundled and pulled into the workspace; transcription ≠ listening, honesty ledger updated everywhere.
25. **46 new memo↔song matches applied** (372 total across 88 songs). Method: title rematch vs the v1.4 catalog + phrase matching requiring chorus-like separated repetition. Verified by transcript reading; cover-practice false positives (The Decline, Kokomo, Lean On Me, Nirvana/MxPx sets, etc.) identified and discarded.
26. **Maxwell Dr 101 (2023-06-20) reattributed to Andrea, not Be With You** — its "be with you" hits come from Andrea's own hook "I know I'll be with you one day". Lake imagery noted as possible Andrea ↔ Home on the Lake link (flagged, not asserted).
27. **9 unknown songs recovered and cataloged (JS-0130…0138)** — every one verified to contain an actual recurring chorus or full lyric in its transcript: I Fall Pretty Down/City's Gone (Oct 2025 — newest), Been Loving You (4 takes/6 months), Don't Put Your Life Away (2021, Mind thread, distinct from the Delilah adaptation), A Place Where You Might, Anyone Can See/Here Is The Church, Chasing Rainbows (Oct 2019 — oldest memo in vault), The Way That You Love Me (cover? — flagged), Happy, Find My Island. Hidden Gems v2 doc supersedes Batch-1 signal guessing.
28. **It's Alright (JS-0128) upgraded to near-finished status**: lyric ~85% reconstructed by triangulating 5 Feb-2022 takes; in the current live set (Oct 2025 practice, 15 chorus reps); provisional potential 74. Named ON DECK for the next WIP slot (when Manic's guitar is tracked). Song Workup doc written. Possible band name-drop "noisy and Something Dirty" flagged for Jeff.
29. **The Mind record now has a complete arc** — It's Alright is its resolution/closer: Manic → Why Do I → Long Long Drive → What To Do → Misery → Take The Step → It's Alright. Logged in Priority Queue as a parked next-year record, not competition for the album.
30. **What To Do bridge: 3 AI-draft sketches delivered** per the Style Guide's missing-section rule (clearly labeled, ORIGINAL untouched, provenance logged). The sanctioned co-write example from Jeff's own charter, executed.
31. **Better Than Now chorus fragments captured** from the 2024-07-25 memo for Sean's confirmation; "Take me away" memos (Jul/Aug 2023) flagged as uncertain earlier stage.
32. **Rad Dad 2025-26 practice tapes transcribe too poorly to mine** (vocals buried under band) — no setlist extraction possible from transcripts; needs a different approach (future: Demucs vocal isolation on set tapes, or Jeff just tells me the setlist).
33. **Momentum Index added to the catalog** (per-original `momentum` 0-100 + `last_activity`, computed from memo dates + doc revisions + mix/bounce dates; 119 originals scored). First audit CONFIRMED the WIP queue (album six = most-alive songs, Jan-Mar 2026 activity). Key finds: Be With You doc edited Feb 2026 (tied-#1 song is alive); Highs and Lows quietly at 14 touches through May 2025; Misery = highest-potential fully-sleeping song → formal #1 revival candidate. One inflation bug caught+fixed (Wham Bam, editorial phrase). Succession logic now explicit: Manic done → It's Alright takes quick-win slot; Turn Over ships → TBFH is flagship.
34. **Gem audio signal-analyzed** (12 memos staged): It's Alright both takes agree C#/Db ~123-129bpm (= D-shapes in Eb tuning); keys/tempos patched into JS-0130..0138 entities.
35. **Two new Workbench scripts delivered for Jeff's Terminal:** basic_pitch_pass.py (13 key memos → MIDI files in "06 Working Copies/MIDI For Logic" to drag into Logic) and chromaprint_pass.py (fingerprint all 916 memos → version-family clusters; resumable). Both read-only on originals.
36. **Dashboard v2 rebuilt:** catalog v1.5, momentum bars + momentum sort (default), last-touched dates, and a Memo Search tab — all 916 transcripts searchable client-side (honesty note about Whisper mishearing included). 776KB self-contained HTML.
37. **Version-chain resolver built** (76 songs, 384 Drive audio/Logic files chained). Logic learned: version numbers RESET across eras (band-session v1.0 2025 ≠ home v4.0 2023), so resolution is by effective date (filename-embedded date beats Drive upload date). One fuzzy-match contamination caught+fixed (Old You ↔ Way I Love You). Every chained song now carries `best_source_resolved` + `latest_file_date`; 11 version-vs-date conflicts auto-detected and resolved by date.
38. **Live-set presence encoded** from 4 setlist docs + the Oct 2025 practice tape: 21 originals carry `live_presence`/`live_latest`. Finds: The Way I Love You played by BOTH bands incl. current 2026 Rad Dad set; **Everyday (ST-0014, unscored, 1999-era) is in the CURRENT 2026 Rad Dad set** — momentum floor 75 applied, listen-flag raised; Drinking Song's "three bands" status now data-backed. Thanksgiving / Head in the Sand / Rabbit Hole confirmed already cataloged.
39. **Machine-checkable rights gates:** every entity now carries `ai_upload_ok` (125 YES Jeff-solo · 21 NO · 2 NEEDS CONSENT — the Greg Baldia co-writes: Mom You're The Bomb + Blue Skies Fade). Suno eligibility is now a lookup, not a judgment call. Gate wording embeds the standing rules (solo recordings only, private, logged).
40. **Next-action audit:** 102 originals had blank/stale next_action — filled by rules from stage + resolved source (released→rests · memo-only→30s listen · lyric-only→hum when it calls · has-mix→rate finish/rest). Every original now has a current next step.
41. **Dashboard v2.1:** LIVE badges (green, with date), AI-upload gate pill (color-coded), auto-resolved latest source, and played-live history per song.

42. **Discovery pass (lyric docs):** 10 unread Drive docs fetched. Cigarettes In A full lyric read + scored 68 (homophone outro). "My muse abuse" linked to Rabbit Hole. Hey There adaptation text confirmed; created same day the Don't Put Your Life Away memos began (germination context). In The End lyric = 4-line fragment. "Unknown song"/"Insane" = archive fragments.
43. **Discovery pass (session transcripts):** Blue Skies Fade OPENING RECOVERED as sung 2024 ("Hello boy, won't you come out to play"). Two new entities: JS-0139 A New Hope (Nov 2024 love song, real lyric), JS-0140 Anthem Part 2 (Oct 2024 protest song). Possible "Treading Water" flagged (2 band sessions, too garbled to promote). Mysteries closed: Music Hall at Fair Park = MJ the Musical attendance; Oi clips = Oi to the World cover takes; My Name Is Steve = comedy bit; Country road rough (Jul 2026) = Country Roads rework (cover-derived, AI-gated NO).
44. **Discovery pass (public web):** "Another Bland EP" found on SoundCloud (May 2023, 5 tracks) — Be With You v2.5 RELEASED there (not homeless), Another Bland Love Song is its title track, EP = 2023 ancestor of the current album push. Christmas releases 2017-2024 cataloged. 1999 album + Trailer Swift too obscure for web search — YT Music playlist remains authoritative; full SoundCloud/YouTube enumeration still blocked on Chrome extension.
45. **Catalog v1.6: 150 entities.** Dashboard + xlsx rebuilt.

46. **Chrome extension retried (Suno hands-on attempt): still not connected.** Standing fix unchanged: full Chrome quit+reopen, signed into claude.ai in the extension.
47. **AI music landscape researched (Aug 2026), Field Guide written.** Suno v5.5 = primary platform (Stem Split/Song Editor upgrade EXP-002's design); free tier lost downloads Nov 2025 → open decision for Jeff: free (listen on-platform) vs Pro $8/mo during experiment months. Udio ruled OUT (post-UMG walled garden, no export). Stable Audio 3 open-weight added to Workbench watchlist (local, private; not installed). Moises/LANDR redundant vs local Demucs/Matchering. Voice-clone wall restated (Suno Personas exist; still forbidden without separate explicit OK). Sony-vs-Suno/Udio rulings expected summer 2026 — decision-aid-only pipeline keeps Jeff's releases fully human and platform-safe (Deezer/Spotify/Apple now tag or filter AI uploads). Rights doc + both EXP briefs updated.

## 2026-08-23 — Integrity pass + StoryBoard path (no audio, no new songs)
48. **ST-0009 Long Long Drive `ai_upload_ok` corrected to YES.** Rights were already `high — CONFIRMED by Jeff 2026-08-18`; the gate had been left on a stale "rights confidence not high" string (exact-match bug). EXP-001 remains approved / Chrome-blocked. No new WIP.
49. **Catalog validator + CI added** (`scripts/validate_catalog.py`). Fail closed on duplicate IDs, score range, gate/rights mismatch, missing original next_action, three-lane cap, protected opus, `app_api.json` drift, audio files in-repo.
50. **StoryBoard is the band OS consumer.** Vault stays the song brain. Import file is `data/app_api.json` (same `songs[]`, mapping under `storyboard`). Not StoryDesk, not StoryOps, no new app. Duration and lead vocalist stay null — Jeff owns feel/set-list.
51. **Counts reconciled:** 150 entities, 126 `classification==original`, 128 YES / 20 NO / 2 NEEDS CONSENT, vm_matches 372/88. Dashboard builder paths un-bitrotted to this repo. Three active songs unchanged. Blue Skies Fade untouched.

## 2026-08-23 — StoryBoard export honesty (no audio, no new songs, no new band)
52. **`app_api.json` field map was wrong vs StoryBoard `catalog-import.ts`.** The importer reads `bpm` (clean int only), constructs `sourceKey` as `vault:catalog_import_v1:{id}`, constructs notes from id/project/original, and sets `active=true`. It does not read `bpm_int`, `vault_ref`, or `is_original` as `active`. Export + docs corrected; `schema_version` 2.
53. **Default StoryBoard live catalog is Rad Dad; this vault labels zero songs that way.** Songs *played* by Rad Dad stay on their catalog projects. `setlist_ready_default_import` is empty on purpose. Do not invent Rad Dad rows. Parked catalogs are not a fourth live band. StoryLiner stays promo only.
54. **Validator fail-closed on the new contract:** missing `app_api.json`, annotated BPM left in `bpm`, field-map lies, fourth live project, YES gate with a co-writer, incomplete `live_presence`, setlist-ready drift, stale export (`--check`). No Music.ai spend. Three active songs unchanged.

## 2026-08-23 — StoryBoard #5 published-slice honesty (no audio, no new band)
55. **Live StoryBoard importer (after StoryBoard #5) prefers `setlist_ready_default_import` and reads `import_scope`.** Claiming those unread, or describing default import as “empty / Rad Dad only,” is a lie against the importer that already dry-runs this public feed (20 live ids). Field map stays StoryBoard `VAULT_STORYBOARD_FIELD_MAP` (`bpm_int`, notes ← `vault_ref`, `active = is_original !== false`). Travis stays `travis_books`.
56. **Empty published slice stays empty.** StoryBoard will not recompute a live band from `played_live` / Jeff Story labels when `setlist_ready_default_import` is present and empty. Validator fails closed if the published 20-id slice, `import_scope=default_live` stamps, and live planner disagree.
57. **No fourth live band, no invented setlist, no audio.** Three active lanes and Blue Skies Fade unchanged. StoryLiner stays promo only.

## 2026-08-23 — StoryBoard #6 parked-named default-live honesty (no audio, no new band)
58. **Live StoryBoard importer (after StoryBoard #6) names the published-slice draft "Vault default-live"** and warns when that slice includes songs whose Vault project is a parked catalog name. Claiming those rows stay off default import, or calling the draft generic "Vault setlist-ready," is a lie against the importer. Everyday (ST-0014 / Stalemate) and Drinking Song (hybrid) stay current-artist repertoire — not a fourth live band.
59. **Validator fails closed** if `default_live_parked_named_ids` / `counts.storyboard_default_live_parked_named` hide those rows, or if setlist names drift from StoryBoard `vaultSetlistIdentity`. No audio. Three active lanes and Blue Skies Fade unchanged.

## 2026-08-23 — setlist_ready vs default-live count honesty (no audio, no new band)
60. **`counts.setlist_ready` could lie after #7.** StoryBoard still treats `setlist_ready` (draft **Vault setlist-ready**) and `setlist_ready_default_import` (draft **Vault default-live**) as two slices. A hand-edit could omit `counts.setlist_ready` or set it equal to the 20-id default-live count while the array still held 40 keyed originals. Validator now requires the count, matches `len(setlist_ready)`, and refuses conflation when the slices differ. The two setlist names stay distinct. Field map, Travis books, parked-named Everyday / hybrid, and the 20-id slice (JS-0128 in; ST-0001 / ST-0002 out) stay as #7 left them.

## 2026-08-23 — published catalog-count honesty (no audio, no new band)
61. **`counts.originals` / `counts.scored` / `counts.ai_upload_ok` could lie after #8.** Those published tallies were not required. A hand-edit could omit them, set `originals` to 40 or 20 (a setlist draft), set `scored` to 126 (every original scored), or set `ai_upload_ok` to 126 (YES == originals) while `songs[]` stayed honest. Validator now requires the three counts, matches classification / potential / YES gates, and refuses those conflations when the numbers differ. Live feed stays 126 ≠ 40 ≠ 20, 59 scored, 128 YES. Field map, Travis books, parked-named Everyday / hybrid, and the 20-id slice stay as #8 left them.

## 2026-08-26 — usable catalog source-of-truth (no audio, no new band)
62. **`data/app_api.json` is the StoryBoard import; `master_catalog.json` is the spine.** StoryBoard's importer still accepts the spine, but without the published slice / `played_live` remap that is not the default-live plan. Claiming the spine is interchangeable is a lie. Validator fails closed if the feed hides that.
63. **Live StoryBoard importer after #9 binds Show Night to planned Vault titles only.** It does not mint excluded or unknown titles, and it does not fill an empty published slice. Vault #7–#10 locked names and counts against a #6-era contract; that leftover is now fail-closed. Nothing auto-posts. Jeff owns feel, set-list, and catalog calls.

## 2026-08-26 — usable catalog local-JSON only (no audio, no new band)
64. **Live StoryBoard after #12 accepts this feed only as local JSON.** Vault #7–#11 already lock names, counts, import-file vs spine, Show Night-does-not-expand, and never_auto_post. The leftover was `ops.remote_catalog_urls=false` published but not fail-closed, while `inspected` still named #9. A hand-edit could claim this private catalog is fetchable, or add a remote locator key that makes Band operations refuse the file. Validator now requires local-JSON-only, rejects remote catalog URLs, and fails closed if the feed itself looks like a locator. Hosted validate may be a 0-step empty-runner — that is not a catalog fail, and billing is unchanged.

## 2026-08-26 — Session Log appears once (no audio, no new band)
65. **Session Log after #12 listed the same local-JSON heading twice.** That leftover made resume dishonest (two identical passes). The #12 section is kept once. Validator fails closed on duplicate Session Log H2 headings. #11/#12 feed locks are unchanged. No second catalog surface.

## 2026-08-26 — Session Log resume from latest (no audio, no new band)
66. **Producer README resume still pointed at Session 1.** After #13, Session Log H2 headings are unique, but resume used the first "NOT done / next continuation point" and the latest honesty passes had no continuation heading. A later session would treat Session 1 as current. Validator now requires Session Log, requires a Next continuation point on the latest H2, and fails closed if Producer README still uses that Session 1 phrase. #11/#12/#13 locks unchanged. Hosted empty-runner is not a catalog fail.

## 2026-08-26 — local validate is the catalog gate (no audio, no new band)
67. **APPS.md still claimed CI fails closed.** After #14, resume is the latest Session Log H2, but APPS.md and the validator docstring still treated hosted CI as the fail-closed gate. Hosted catalog-validate on this private repo may be a 0-step empty-runner. Local validate is the gate. Validator now fails closed if APPS.md or the validator docstring still claims CI fails closed, and APPS.md must admit the empty-runner is not a catalog fail. #11/#12/#13/#14 locks unchanged. Do not change billing.

## 2026-08-26 — Jeff-facing docs roles and counts only (no audio, no new band)
68. **README.md and APPS.md still dumped catalog leftovers after #15.** Local validate was already the catalog gate. The leftover was Jeff-facing ecosystem wording: live-lane titles, protected-opus names, collaborator-as-catalog-map dumps, and write-back ids. Those docs are roles and counts only. Validator fails closed if they leak again. The #15 empty-runner lock is unchanged. No public catalog dump. Do not change billing.

## 2026-08-27 — local validate success is roles and counts only (no audio, no new band)
69. **Catalog-gate success line still printed published lane ids after #16.** Jeff-facing docs were already roles and counts only. The leftover was the local validate success report: it still echoed published lane ids, so verify output could not be pasted without sanitizing. Success is now roles and counts only. Validator fails closed if that line ships published ids. The #16 public-doc lock is unchanged. The #15 empty-runner lock is unchanged. Do not change billing.

## 2026-08-27 — StoryBoard rejects the spine (no audio, no new band)
70. **Catalog tooling still planned a spine import after #17.** Jeff-facing docs and catalog-gate success were already roles and counts only. Live StoryBoard #16 rejects `master_catalog.json` at the import boundary, but the contract still computed a non-empty spine plan and APPS.md still described the deleted remap path. Validator now fails closed if the feed hides that reject, if inspected omits StoryBoard #16, or if APPS.md still claims the spine-accept path. The #17 success-line lock is unchanged. The #16 public-doc lock is unchanged. The #15 empty-runner lock is unchanged. Do not change billing.
